<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="/styles/main.css" rel="stylesheet"/>
   
    
</head>
<body>
  
    
       <%- include("partials/background.ejs") %>
       <!-- <div class="bigContainer"> -->
        <div class="container weatherInfo weather-info-container">
            <div class="p-5 text-center rounded-5 box">
              <img class="text-center bi mt-4 mb-5" src="<%= imgSrc %>" alt="clear day">
              <h1 class="mb-5  header"><%= locals.weather %></h1>
              <div class="layout">
              <div>
                  <!-- <h3 class="text-body-emphasis">Minimum Temperature</h3> -->
                  <img class="thermometers bi mt-4 mb-3" src="/assets/thermometer-snow.svg" alt="Minimum temperatures thermometer">
                  <h4 class="text-body-emphasis"><%= minTemp %></h4>
              </div>
              <div>
                <!-- <h3 class="text-body-emphasis">Maximum Temperature</h3> -->
                <img class="thermometers bi mt-4 mb-3" src="/assets/thermometer-sun.svg" alt="Maximum temperatures thermometer">
                <h4 class="text-body-emphasis"><%= maxTemp %></h4>
              </div>
            </div>
              
              <form id="dayForm" action="/currentDay" method="POST">
                <select form="dayForm" name="choice" class="form-select daySelector" aria-label="Default select example">
                  <option value="100" class="selection" selected>Currently selected date : <%= currentDay %> </option>
                  <% let counter = 0 %>
                  <% data.daily.time.forEach(day => { %>
                    
                    <option value="<%= counter %>"><%= day %></option>
                    <% counter ++ %>
                    <% }); %>
                </select>
                <button class="d-inline-flex align-items-center btn btn-primary btn-lg px-4 rounded-pill checkWeather mt-5" type="submit">
                  Check Weather
                 </button>
              </form>
              
              <div class="d-inline-flex gap-2 mb-5">
              </div>
              
            </div>
          </div>
        <!-- </div>  -->
   
</body>
</html>